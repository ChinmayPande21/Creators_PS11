# Backend

This folder is reserved for the backend/API for ClearFix.

Suggested next steps (optional):

- Initialize a Node/Express server here
- Add routes for auth, complaints, payments, and ragging alerts
- Connect a database (SQLite/Postgres) instead of localStorage
